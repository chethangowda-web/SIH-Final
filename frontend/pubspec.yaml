name: pds_demandsync
description: "PDS DemandSync - Forward-Looking Beneficiary Intent for Pre-Dispatch PDS Demand Forecasting"
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  crypto: ^3.0.7
  firebase_auth: ^6.7.0
  firebase_core: ^4.15.0
  flutter:
    sdk: flutter
  flutter_map: ^8.3.1
  http: ^1.2.1
  image_picker: ^1.2.3
  intl: ^0.19.0
  latlong2: ^0.10.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/images/
